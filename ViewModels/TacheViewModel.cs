namespace TaskAii.ViewModels
{
    public class TacheViewModel
    {
        private Tache tache;

        public TacheViewModel(Tache tache)
        {
            this.tache = tache;
        }

        // Propriétés et méthodes pour interagir avec la vue
    }
}